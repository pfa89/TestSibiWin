{
  "name": "test-sibi-win",
  "version": "1.0.0",
  "description": "Playwright API tests for the Quality Ladles WordPress site (test.sibi.win)",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "keywords": [
    "playwright",
    "api",
    "wordpress",
    "woocommerce"
  ],
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.5"
  }
}
